<table class="table table-striped">
  <thead>
    <tr>
      <th>Count</th>
      <th>Tag</th>
    </tr>
  </thead>

  <tbody>
    <% @tags.each do |tag| %>
      <tr>
        <td>
          <span class="badge badge-default"><%= tag.taggings_count %></span>
        </td>
        <td>
          <%= link_to tag, tag_url(tag.name) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
